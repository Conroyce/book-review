<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <a href="/books"><h1>Books</h1></a>
   
      
    </div> 
    <div id="navbar" class="navbar-collapse collapse">
      <% if current_user %>
        <div class="navbar-right">
          Welcome, <%= current_user.name %>!
          <%= link_to "Sign Out", log_out_path %>
        </div>  
      <% else %>   
        <%= form_tag log_in_path, class: "navbar-form navbar-right" do %>
          <div class="form-group">
            <%= text_field_tag :name, nil, placeholder: "Username", class: "form-control" %>
          </div>  
          <div class="form-group">
            <%= password_field_tag :password, nil, placeholder: "Password", class: "form-control" %>
          </div>  
            <%= submit_tag "Log in", class: "btn-success btn" %> 
        <% end %> 
        <%= link_to "Sign Up", users_path, class: "navbar-right" %>
      <% end %>
    </div>
  </div>
</nav>
<div class="col-md-12 space-8">
</div>

<div class="col-md-4">
</div>
<div class="col-md-4 space-4-bot">
  <h3>Search For Books</h3>
  <div class="search-div">
      <input type="text" placeholder="Search Books" id="searchBox" name="searchBox" class="form-control">
      <input type="submit" value="Search" class="searchButton form-control btn-primary">
  </div>
  
</div>
<div class="col-md-4">
  <% if current_user %>
       <%= link_to "My Reviews", user_messages_path(current_user.id) %>
     <% end %> 
</div>

<div class="col-md-2">
</div> 
<div class="col-md-8">
  
</div>  
<div class="col-md-2">

</div>
<div class="container main-back">
  <div class="main">
  </div>
</div>  

<script id="book-template" type="text/x-handlebars-template">
  <div class="entry">
    {{#each this.books}}
      <div class="col-md-2 even-div">
        <a href="/books/{{id}}" class="show-book"><div class="overlay-wrapper">
          <span class="focus-overlay"></span><img src={{img}} height="200" width="150"><br>
        </div></a>  
        <div class="book-info">
          <a href="/books/{{id}}" class="show-book"><p class="title">{{title}}</p></a>
          <a href="/books/{{id}}" class="show-book"><p class="authors">{{authors}}</p></a>
          <input type="hidden" value="{{id}}" name="{{id}}" class="bookId"><input type="hidden" value="{{title}}" name="{{title}}" class="bookTitle"></a>
          <p class="{{id}} addBook"><a href="/books" class="addBook">Add To Favorites<input type="hidden" value="{{id}}" name="{{id}}" class="bookId"><input type="hidden" value="{{title}}" name="{{title}}" class="bookTitle"></a></p>
        </div>  
      </div>
    {{/each}}
  </div>
</script>